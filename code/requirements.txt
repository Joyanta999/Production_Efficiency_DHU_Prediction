numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
# optional: the official RTDL package (we ship a self-contained FT-Transformer /
# ResNet implementation in models.py so this is NOT required, but you may swap
# them in if you prefer the reference implementation):
# rtdl>=0.0.13
